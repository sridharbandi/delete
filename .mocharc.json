{
    "spec": "tests/**/*.spec.ts",
    "require": "./loader.js",
    "reporter": ["spec"],
    "reporter-options": [
        "reportDir=reports",
        "reportFilename=index",
        "json=false"
    ]
}