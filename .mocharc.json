{
    "spec": "tests/**/*.spec.ts",
    "require": "./loader.js",
    "reporter": ["mocha-junit-reporter"],
    "reporter-options": [
        "reportDir=reports",
        "reportFilename=index",
        "json=false"
    ]
}